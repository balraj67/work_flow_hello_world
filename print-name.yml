name: print-name
on:
  push:
    branches:
      - main
jobs:
  say-hello:
    runs-on: unbuntu-latest
    steps:
      - name: Print Hello Message
        run: echo "Hello from the print-name workflow"
